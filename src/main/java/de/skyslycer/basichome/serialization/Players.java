package de.skyslycer.basichome.serialization;

import java.util.HashMap;
import java.util.UUID;

public class Players extends HashMap<UUID, Homes> { // This stores the home list (HomeData) mapped to the owning player

}
