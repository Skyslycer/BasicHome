package de.skyslycer.basichome.serialization;

import java.util.HashMap;
import java.util.UUID;

public class PlayerData extends HashMap<UUID, HomeData> { // This stores the home list (HomeData) mapped to the owning player

}
